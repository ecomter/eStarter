<UserControl x:Class="eStarter.Views.AboutPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="600" Width="1000">
    <Border Background="{DynamicResource MetroBackgroundBrush}" Padding="30,0,30,30">
        <StackPanel>
            <!-- Header matches MainWindow style -->
            <TextBlock Text="About" Style="{StaticResource MetroHeaderTextStyle}"/>
            
            <StackPanel Margin="0,20,0,0">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <TextBlock Text="eStarter" Style="{StaticResource MetroSubheaderTextStyle}" Margin="0,0,10,0"/>
                    <TextBlock x:Name="VersionText" Style="{StaticResource MetroBodyTextStyle}" VerticalAlignment="Bottom" Margin="0,0,0,4"/>
                </StackPanel>

                <TextBlock Style="{StaticResource MetroBodyTextStyle}" Margin="0,0,0,20">
                    eStarter 是一款轻量级的启动器与应用管理器，旨在让常用应用与任务在桌面环境中更快捷地访问与管理。
                </TextBlock>

                <TextBlock Text="Features" Style="{StaticResource MetroSubheaderTextStyle}" Margin="0,20,0,10"/>
                <TextBlock Style="{StaticResource MetroBodyTextStyle}">
                    • 快速启动：通过可自定义的瓷砖与快捷方式，一键启动常用程序。<LineBreak/>
                    • 个性化布局：支持调整瓷砖大小、颜色与排列，满足不同桌面风格。<LineBreak/>
                    • 简洁的安装体验：集成示例应用安装器，便于演示与上手。<LineBreak/>
                    • 资源占用低：采用轻量化架构，兼顾速度与响应性。
                </TextBlock>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>