<UserControl x:Class="eStarter.Views.Settings.UsersPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="Microsoft YaHei, Segoe UI">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,20,0">
        <StackPanel Margin="0,0,0,40">
            <!-- Header -->
            <TextBlock Text="Users" 
                       FontFamily="Microsoft YaHei Light, Segoe UI Light"
                       FontSize="42" FontWeight="Light"
                       Foreground="{DynamicResource MetroForegroundBrush}"
                       Margin="0,0,0,30"/>

            <!-- Your Account -->
            <TextBlock Text="Your account" FontSize="18" FontWeight="Light"
                       Foreground="{DynamicResource MetroAccentBrush}" Margin="0,0,0,20"/>
            
            <Grid Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- User Picture -->
                <Border Grid.Column="0" Width="120" Height="120" Background="#FF444444"
                        CornerRadius="60" Margin="0,0,20,0">
                    <Grid>
                        <TextBlock Text="&#xE13D;" FontFamily="Segoe UI Symbol" FontSize="48"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Foreground="#99FFFFFF"
                                   Visibility="{Binding UserPicture, Converter={StaticResource NullToVisibilityConverter}}"/>
                        <Image Source="{Binding UserPicture}" Stretch="UniformToFill"
                               Visibility="{Binding UserPicture, Converter={StaticResource NotNullToVisibilityConverter}}">
                            <Image.Clip>
                                <EllipseGeometry Center="60,60" RadiusX="60" RadiusY="60"/>
                            </Image.Clip>
                        </Image>
                    </Grid>
                </Border>
                
                <!-- User Info -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="{Binding UserName}" FontSize="24" FontWeight="Light"
                               Foreground="{DynamicResource MetroForegroundBrush}"
                               Margin="0,0,0,5"/>
                    <TextBlock Text="Local Account" FontSize="14"
                               Foreground="{DynamicResource MetroForegroundBrush}" Opacity="0.7"/>
                </StackPanel>
            </Grid>

            <!-- Change Picture -->
            <TextBlock Text="Change your picture" FontSize="13" 
                       Foreground="{DynamicResource MetroForegroundBrush}" Margin="0,0,0,10"/>
            
            <!-- Recent Pictures (placeholder) -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                <Border Width="60" Height="60" Background="#FF444444" Margin="0,0,8,0"/>
                <Border Width="60" Height="60" Background="#FF444444" Margin="0,0,8,0"/>
                <Border Width="60" Height="60" Background="#FF444444" Margin="0,0,8,0"/>
                <Border Width="60" Height="60" Background="#FF444444" Margin="0,0,8,0"/>
                <Border Width="60" Height="60" Background="#FF444444" Margin="0,0,8,0"/>
            </StackPanel>
            
            <Button Content="Browse" 
                    Width="100" Height="32"
                    HorizontalAlignment="Left"
                    Background="{DynamicResource MetroSeparatorBrush}"
                    Foreground="{DynamicResource MetroForegroundBrush}"
                    BorderThickness="0" Cursor="Hand"
                    Command="{Binding BrowsePictureCommand}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" Padding="16,6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource MetroAccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <!-- Sign-in Options -->
            <TextBlock Text="Sign-in options" FontSize="18" FontWeight="Light"
                       Foreground="{DynamicResource MetroAccentBrush}" Margin="0,40,0,15"/>
            
            <TextBlock Text="Create a picture password or PIN for faster sign-in." 
                       FontSize="13" Foreground="{DynamicResource MetroForegroundBrush}"
                       Opacity="0.8" Margin="0,0,0,15"/>
            
            <StackPanel Orientation="Horizontal">
                <Button Content="Create a picture password" 
                        Height="32" Padding="16,0"
                        Background="{DynamicResource MetroSeparatorBrush}"
                        Foreground="{DynamicResource MetroForegroundBrush}"
                        BorderThickness="0" Cursor="Hand" Margin="0,0,10,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="{DynamicResource MetroAccentBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                
                <Button Content="Create a PIN" 
                        Height="32" Padding="16,0"
                        Background="{DynamicResource MetroSeparatorBrush}"
                        Foreground="{DynamicResource MetroForegroundBrush}"
                        BorderThickness="0" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="{DynamicResource MetroAccentBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
