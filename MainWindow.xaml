<Window x:Class="eStarter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:eStarter"
        xmlns:models="clr-namespace:eStarter.Models"
        xmlns:conv="clr-namespace:eStarter.Converters"
        mc:Ignorable="d"
        Title="eStarter" Height="600" Width="1000">
    <Window.Resources>
        <conv:BadgeVisibilityConverter x:Key="BadgeVisibilityConverter" />

        <DropShadowEffect x:Key="DropShadow" BlurRadius="10" ShadowDepth="2" Opacity="0.4" />

        <Style x:Key="TileButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="150" />
            <Setter Property="Height" Value="150" />
            <Setter Property="Margin" Value="8" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="10" Effect="{StaticResource DropShadow}" RenderTransformOrigin="0.5,0.5">
                                <Border.RenderTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1" />
                                </Border.RenderTransform>
                            </Border>
                            <Grid Margin="8">
                                <StackPanel>
                                    <TextBlock x:Name="Title" Text="{Binding Name}" FontWeight="Bold" FontSize="16" TextWrapping="Wrap" HorizontalAlignment="Center" />
                                    <TextBlock x:Name="Desc" Text="{Binding Description}" FontSize="12" Opacity="0.9" TextWrapping="Wrap" HorizontalAlignment="Center" Margin="0,6,0,0" />
                                </StackPanel>
                            </Grid>

                            <Border x:Name="Badge" Background="#FFDD2C00" Width="28" Height="28" CornerRadius="14" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="-8,-8,8,0" Visibility="Collapsed">
                                <TextBlock x:Name="BadgeText" Text="{Binding BadgeCount}" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Bd" Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1.05" Duration="0:0:0.12" />
                                            <DoubleAnimation Storyboard.TargetName="Bd" Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1.05" Duration="0:0:0.12" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Bd" Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.12" />
                                            <DoubleAnimation Storyboard.TargetName="Bd" Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.12" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <DataTrigger Binding="{Binding BadgeCount}" Value="0">
                                <Setter TargetName="Badge" Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding BadgeCount}" Value="1">
                                <Setter TargetName="Badge" Property="Visibility" Value="Visible" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding BadgeCount}" Value="2">
                                <Setter TargetName="Badge" Property="Visibility" Value="Visible" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding BadgeCount}" Value="3">
                                <Setter TargetName="Badge" Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TileEntrance" TargetType="Border">
            <Setter Property="Opacity" Value="0" />
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <TranslateTransform Y="10" />
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.35" />
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TranslateTransform.Y)" From="10" To="0" Duration="0:0:0.35" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <DockPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="12">
                <TextBlock Text="eStarter" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Margin="6" />
                <Button Content="Refresh" Command="{Binding RefreshCommand}" Margin="12,0" />
                <Button Content="Install sample" Command="{Binding InstallCommand}" Margin="6,0"/>
            </StackPanel>

            <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding InstalledApps}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel IsItemsHost="True" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:AppEntry}">
                            <Border Style="{StaticResource TileEntrance}" RenderTransformOrigin="0.5,0.5">
                                <Button Style="{StaticResource TileButtonStyle}" Command="{Binding DataContext.LaunchCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <Border Background="{Binding Background}" CornerRadius="6" Grid.Row="0" />

                                        <StackPanel Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16" Foreground="White" TextAlignment="Center" />
                                            <TextBlock Text="{Binding Description}" FontSize="12" Foreground="#DDFFFFFF" TextAlignment="Center" Margin="4,6,4,0" />
                                        </StackPanel>

                                        <Border Width="30" Height="30" Background="#FFDD2C00" CornerRadius="15" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,8,8,0" Visibility="{Binding BadgeCount, Converter={StaticResource BadgeVisibilityConverter}}">
                                            <TextBlock Text="{Binding BadgeCount}" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" />
                                        </Border>
                                    </Grid>
                                </Button>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </DockPanel>
    </Grid>
</Window>
